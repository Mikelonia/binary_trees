#include "binary_trees.h"

/**
 *
 * There is nothing here therein.
 */
/**
 * heap_to_sorted_array - ?
 * @heap: ?
 * @size: ?
 * Return: ?
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	(void) heap;
	(void) size;
	return (0);
}
